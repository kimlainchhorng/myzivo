/** Scheduled bookings stub */
export function useScheduledBookings() {
  return { bookings: [], isLoading: false, error: null };
}
export function useScheduledBookingsQuery(..._args: any[]) {
  return { data: [], isLoading: false, error: null };
}
