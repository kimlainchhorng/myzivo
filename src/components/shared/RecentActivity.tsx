/**
 * RecentActivity - Removed fake social proof notifications
 * This component previously showed fabricated user activity.
 * It now renders nothing. Remove imports if no longer needed.
 */
const RecentActivity = () => {
  return null;
};

export default RecentActivity;
